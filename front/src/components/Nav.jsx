import React from 'react';

const Nav = (props) => {
  // ... código del componente Nav

  return (
    <div>
      {/* ... otros elementos del componente Nav */}
    </div>
  );
};

export default Nav;